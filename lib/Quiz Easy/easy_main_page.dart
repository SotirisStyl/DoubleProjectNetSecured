import 'package:flutter/material.dart';

class EasyMainPage extends StatelessWidget {
  const EasyMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Easy Mode Quiz Page')));
  }
}