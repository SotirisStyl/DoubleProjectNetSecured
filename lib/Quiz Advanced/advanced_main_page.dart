import 'package:flutter/material.dart';

class AdvancedMainPage extends StatelessWidget {
  const AdvancedMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Advanced Mode Quiz Page')));
  }
}