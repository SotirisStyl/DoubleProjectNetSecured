import 'package:flutter/material.dart';

class MediumMainPage extends StatelessWidget {
  const MediumMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Easy Mode Quiz Page')));
  }
}